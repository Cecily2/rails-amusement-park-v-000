<%= @attraction.name  %>

<%= link_to "Edit Attraction", edit_attraction_path(@attraction) if current_user.admin %>

<%= form_tag "/attractions/#{@attraction.id}/ride" do %>
  <%= submit_tag "Go on this ride" %>
<% end %>